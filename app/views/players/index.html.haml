%table
  %tbody
    %tr
      %th Id
      %th Name
      %th Level
      %th Actions
    - @players.each do |player|
      %tr
        %td= player.id
        %td= link_to player.name, player_path(player)
        %td= player.level
        %td Delete/Edit
